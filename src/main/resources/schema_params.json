{
  "type": "object",
  "properties": {
    "detectors": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["01", "02", "03", "04", "05", "06", "07", "08", "09", "10", "11", "12"]
      },
      "description": "List of all the detectors we want to process, allowed elements are string between 01 and 12"
    },
    "bands": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["B01", "B02", "B03", "B04", "B05", "B06", "B07", "B08", "B8A", "B09", "B10", "B11", "B12"]
      },
      "description": "List of all the bands we want to process, allowed elements are string between B01 and B12."
    }
  }
}